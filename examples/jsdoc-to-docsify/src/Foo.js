/**
 * @function thisAndThat - This awesome thisAndThat function does this and that.
 *
 * Here's a simple example:
 *
 * ```js
 * thisAndThat('yoohoo', Math.PI)
 * ```
 *
 * @param {string} a - The a param should be this.
 * @param {number} b - The b param should be that.
 * @returns {boolean}
 */
export function thisAndThat(a, b) {
	console.log(a, b)
	if (Math.round(Math.random())) return true
	else return false
}

/**
 * @function magic - Does some magic. For example:
 *
 * ```js
 * magic('cool') // Something magic happens
 * ```
 *
 * @param {string} bean The type of the magic beans to use, f.e. "cool"
 * @return {void} - Magic side effects, no return to reality.
 */
export function magic(beans) {
	//
}
