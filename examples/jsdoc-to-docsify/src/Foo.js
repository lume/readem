/**
 * @function foo - This awesome foo function does this and that.
 *
 * Here's a simple example:
 *
 * ```js
 * foo('yoohoo', Math.PI)
 * ```
 *
 * @param {string} a - The a param should be this.
 * @param {number} b - The b param should be that.
 * @returns {boolean}
 */
export function foo(a, b) {
	console.log(a, b)
	if (Math.round(Math.random())) return true
	else return false
}
